import React from 'react';
import { LoginContainer, LoginBg } from './login-styles';

const Login = () => {
  return (
    <LoginBg>
      <LoginContainer />
    </LoginBg>
  );
};

export default Login;
